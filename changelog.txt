-----------
Known  Bugs
-----------

(!!) Offset problem
(!) Stack leniency is somewhat broken
When importing maps in fullscreen the window doesnt show up
Cant hit objects before map starts

After deleting a mapset, it tries to load the deleted mp3

-----------
   To do
-----------

# Refactor massive functions 
# Make a split helper (Why didn't i do this earlier...) 

db.cpp : update map imports & db reconstruction to update the screen (instead of not responding the whole time)
ingame.cpp : improve bezier slider rendering, unnecessary draws on corners
song_select.cpp : add right click on maps for good gui delete
		  add search function
		  improve background rendering (no scaling just crop, parallax effects)
settings.cpp : add an option for background rendering in-game
	       add an option for slider resolution for potentially better performance
	       add an option to toggle slider-end rendering

-----------
 Changelog
-----------

a2025.1118

(*) Fixed crash on trying to enter song select with an invalid db

a2025.1120

(*) Fixed slider endpoint positions
(+) Made song select screen scale with resolution
(+) Added settings screen
(~) Started work on slider rendering

a2025.1121

(~) Slider rendering progress

a2025.1123

(+) Custom cursor drawing
(+) Added a basic init screen

a2025.1124

(+) Slider rendering done
(*) Fixed crash on result screen for old computers

a2025.1125 (release alpha v0.0.1)

(~) Slider rendering fixes & slider logic progress
(*) Fixed slider end times
(*) Fixed a database crash

a2025.1126

(+) Added stack leniency
(*) Database version 8: more map metadata stored
(*) Fixed database LastID bug
(*) Fixed a map import bug
(*) Fixed crash after map end in some cases
(*) Fixed different mp3s on the same mapset not being played on the song select screen
(Code) Fixed annoying building bug
(Code) A few minor code changes

a2025.1128 

(+) Implemented slider ticks
(+) Implemented slider logic

a2025.1129
(Code) Got rid of most warnings in the code

a2025.1201
(+) Add setting to toggle sliderend rendering
(*) Fixed short sliders' ends fucking up
(*) Fixed sliderheads not disappearing after hit

a2025.1205 (alpha 0.1.1) - huge milestone!
(+) Implemented reverse arrows - done with slider logic!
(+) Implemented spinner logic
(+) Implemented score logic
(*) Tweaked notelock a bit (behaves more like lazer)
(*) Fixed a slider ball tracking bug
(*) Fixed total map lengths
(*) Fix sliderticks not disappearing after hit
(*) Fix some slider reverse arrows looking the wrong way
(Code) Added an "add hit" helper

a2025.1211

(~) Started work on hitsounds
(*) Try fixing song offset issue

a2025.1219

(+) Add katu and gekis
(~) More work on hitsounds

a2025.1225

(+) Hitsounds work now (no custom hitsounds yet)

a2025.1229

(*) Inverted song offset "fix"

a2025.1231

(+) Result screen done
(+) Hit results are now textured and they fade out
(Code) Refactored atlas access (no more magic numbers)

a2026.0101

(+) Various small QoL additions like combo break sound
(+) Implemented UR bar
(+) Implemented fps/ms counter
(+) Added UR info to results screen

a2026.0105

(~) Progress on settings screen
(+) Added raw input toggle
(*) Fixed crash on maps with only spinners

a2026.0220

(+) Implemented dropdown lists on settings screen
(+) Added a few misc. settings like cursor size
(+) Added volume controlst
(*) Fixed numbers on top of circles not fading in correctly
(*) Added higher resolution images for result screen

a2026.0222

(~) Re-did slider rendering; now much faster and more accurate. 
(+) Added pausing in game
(+) Added submenus on song select screen
(+) Added sensitivity setting
(+) Added mod select screen
(+) Made mods Easy, HalfTime, HardRock, DoubleTime(partial), NightCore (partial), Hidden functional
(*) Fixed offset issue when skipping
(*) Fixed spinner's middle circle not scaling with resolution
(*) Fixed some sliders not rendering
(*) Fixed slidertick appearance
(*) Fixed sliderticks after a reverse arrow not showing up
(*) Fixed sliders playing wrong sounds
(*) Fixed slider transparency (thanks to the slider render rewrite)

a2026.0227

(+) Added in-game key overlay (toggleable)
(+) Made mod Flashlight functional
(*) Fixed sprites bleeding through each other
(*) Fixed slider approach circles still rendering after being hit early
(Code) New sprite atlas format & loader (switched to gdx texture packer)

a2026.0228

add toggleable key overlay
add remaining mods functionalities
fix slider red ticks going too crazy on sharp corners
ADD Offset controls
add toggles for permanent fl 100/200 combo
add toggleable followpoints 
add score saving
for modifying map lists: always load maps in same order, use another file to keep track of grades/unplayed status, load by index? probably a terrible idea

fix:
Keys dont block mouse clicks as they should
map speed fucking up spinners
slider hit sounds can fuck up at soft/drum/normal transitions

add:
(DONE) FINISH RESULT SCREEN 
(DONE) break combo sound
(DONE) render 300s toggle
(DONE) fps/ms overlay
(DONE) SENSITIVITY & RAW INPUT!!!
(DONE) Toggleable UR bar
(DONE) keep UR data to show at end of play
(DONE) toggle rectangle that shows play area
(DONE) sound control
(DONE) fix number transparencies on notes

(wip) mods (with rate change)
custom sounds
offline profile
osu api support

change key settings (grid/list)
A good main menu
menu transitions so it doesnt look shit.

record replay (toggleable for performance)
a benchmark button (auto mod prerequisite) would be cool


some sliders end a bit too early (i think they might be missing the last point in the path), their sliderend hitsound volume is wrong, and some reverse arrows still change directions rarely. fix it (oyasumi hard has a bunch of wrong reverse arrows - maybe fix i used for perfect circle sliders is applicable here(beziers) as well - can i do this at map creation instead of ::draw though) 
also bbydoll has some sliders that dont show up at all wtf
weird slider head on die young NYAN diff middle part

hp (toggleable) (ON HOLD)


things to aim for in beta milestone:
change key settings, replay record, all mods working, no major bugs, better song select, better main menu & menu transitions.

for release:
offline profile, osu api support, custom map sfx support, (partial) custom skin support (most ambitious), benchmark button

